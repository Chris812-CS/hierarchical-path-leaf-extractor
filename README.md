# hierarchical-path-leaf-extractor

Converts hierarchical path rows into branch-aware paths and keeps only the deepest (leaf) rows, e.g., `Cat1 | Function1 | Brand1 | Product1 | SubCat1`.

**Raw File:** `hierarchical dataset.xlsx`  
**Output File:** `hierarchical_leaf_only.xlsx`
